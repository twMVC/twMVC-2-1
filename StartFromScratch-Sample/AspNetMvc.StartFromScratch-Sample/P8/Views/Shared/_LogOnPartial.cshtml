@if (Request.IsAuthenticated)
{
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Log Off", "LogOff", "Account") ]</text>
}
else
{
    @*因為沒有需要額外資枓，使用RenderPartial呈現資料*@
    @Html.Partial("~/Views/Account/Login.cshtml",null);
}
